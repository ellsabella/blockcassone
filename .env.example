OPENSEA_API_KEY=
OPENSEA_DEFAULT_WALLET=
ETH_RPC_URL=
# Attestation signer service (/api/attest) — the signer's PRIVATE key (its address must
# equal the deploy-time BLOCKCASSONE_ATTESTATION_SIGNER). Never the deployer key. Only the
# Sepolia/prod wallet-upload Update path needs this; local Anvil uses its unlocked signer.
BLOCKCASSONE_ATTESTATION_SIGNER_PK=
